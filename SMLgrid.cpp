#include "SMLgrid.h"

void MLGrid::createGrid(
        /*std::vector< float >* input,
        std::vector<int> nodes_nums,
        std::vector<float(*)(const float&)> activation_functions*/
        )
{
    //input = std::vector<float>(input_size);
}